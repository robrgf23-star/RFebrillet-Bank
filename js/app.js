// ==========================
// RFEBRILLET BANK - APP.JS
// Lógica de inicialización general, si se requiere.
// ==========================

document.addEventListener('DOMContentLoaded', () => {
    console.log("RFebrillet Bank - Aplicación base inicializada.");
    
    // Ejemplo de funcionalidad: manejar scroll suave para anclas (ya definido en base.css, 
    // pero aquí se podría añadir lógica extra si fuera necesario)
});
